# my first python code
# don't use ;
# variables have to start with letters and can have numbers
# idention are important! if something is more idented than
# other thing it means that is inside the other, look line 10 and 11
a1 = 2
b1 = 3
sum = a1 + b1
print(sum)
if sum > 4:
    print("greater than 4")
